import {PdfPageDirective} from './pdf-page.directive';

describe('PdfPageDirective', () => {
  it('should create an instance', () => {
    const directive = new PdfPageDirective();
    expect(directive).toBeTruthy();
  });
});
